const AcceptInput = () => {
  return (
    <label htmlFor="acceptTerms">
      <input type="checkbox" name="accept" id="acceptTerms" />I accept the Terms
      and Conditions
    </label>
  );
};

export default AcceptInput;
